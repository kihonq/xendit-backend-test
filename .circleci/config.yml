version: 2
jobs:
  build:
    docker:
      - image: circleci/node:16.15.0
    steps:
      - checkout
      - restore_cache:
          key: v2-npm-deps-{{ checksum "package.json"}}
      - run:
          name: Install deps
          command: npm install
      - save_cache:
          key: v2-npm-deps-{{ checksum "package.json"}}
          paths:
            - ./node_modules
      - run:
          name: Lint & Test
          command: npm test && npm run codecov
      - store_artifacts:
          path: test-results.xml
      - store-artifacts:
          path: coverage
          prefix: coverage
      - store_test_results:
          path: test-results.xml
      - run:
          name: upload to codecov
          command: bash <(curl -s https://codecov.io/bash)